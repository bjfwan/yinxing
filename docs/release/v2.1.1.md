# 🍂 银杏 v2.1.1 发布

发布日期：2026-09-12

🔌 无障碍连接判断：区分“系统开关未开启”和“开关已开启但服务尚未连接”，连接异常时立即提示恢复，不再错误地告诉用户权限未开启，也不再静默等待到总超时。

🎥 微信通话确认：在微信页面结构或窗口类不可见时，增加通话前后音频通信状态变化作为确认依据；仍需连续两次确认，视频选项弹窗未消失时不会提前判定成功。

🧭 失败链路诊断：日志与性能指标增加构建 SHA、构建类型、包名、源码状态、会话标识和递增事件序号；微信失败样本始终保留可解析的失败步骤。

⏱️ 卡顿诊断：记录主线程实际阻塞时长；采样时已经恢复到系统空闲队列的情况只记恢复指标，不再作为新的错误样本。

🔐 隐私收紧：联系人姓名和电话号码不再写入缓冲日志，诊断字段继续保持脱敏、有界和可聚合。

📦 发布追溯：正式包从干净 Git 提交生成，并把签名 APK 与同次 R8 `mapping.txt` 绑定归档，线上堆栈可以对应到具体源码。

✅ 工程验证：`817 tests, 0 failed`；网站与品牌契约除待发布版本同步项外均通过。

⚠️ 验证边界：本版本的软件回归与构建门禁已完成；华为真机上的无障碍后台断连和微信页面差异仍需依靠更新后的真实用户样本继续验证。本版本不包含热更新或静默安装。
